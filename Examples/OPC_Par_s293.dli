generate   10000
produce    20
seed 293 
title "DOP as a condition  "
opener west

condition (
	( spades( west ) + spades( east ) >= 8 )
	 && (opc(EW, spades) >= 20.5 )  
  )

action printrpt(EW , opc(EW),  hcp(east)+hcp(west) ,par(EW,both))   
