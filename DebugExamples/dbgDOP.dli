generate   10000
produce    2
seed 11
title "DOP as a condition  "
opener west

condition (
	( spades( west ) + spades( east ) >= 8 )
	 && (opc(EW, spades) >= 20.5 )  
  )

action printrpt(EW , opc(EW),  hcp(east)+hcp(west) ,par(EW,both))   // opc(EW,notrump),
